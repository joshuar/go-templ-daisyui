// Copyright 2024 Joshua Rich <joshua.rich@gmail.com>.
// SPDX-License-Identifier: 	MIT

package components

templ (i Image) Show() {
	<img
		class={ templ.KV(i.setMask(), i.mask > 0),
			templ.KV(i.setFit(), i.fit > 0) }
		src={ i.url }
		if i.alt != "" {
			alt={ i.alt }
		}
	/>
}
